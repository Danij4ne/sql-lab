
-------------------------------------- DATA WRITING ---------------------------------------------------------
-- Commands that actually modify the database

-- INSERT INTO = insert data into a table
-- VALUES()    = the values that the row will have

INSERT INTO users (user_id, name, surname)
VALUES (7, 'Maria', 'Lopez');
-- Insert into the users table (user_id, name, surname)
-- the VALUES (7, 'Maria', 'Lopez').
